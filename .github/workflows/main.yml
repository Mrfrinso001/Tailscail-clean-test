name: Debug Tailnet API
on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Curl API test
        env:
          APIKEY: ${{ secrets.TAILSCALE_API_KEY }}
        run: |
          echo "👉 Testing API against mrfrinso001.github"
          curl -v -H "Authorization: Bearer $APIKEY" \
            https://api.tailscale.com/api/v2/tailnet/mrfrinso001.github/machines
